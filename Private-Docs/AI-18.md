# AI-18 — EWS MIME attachments (digest PDF fix)

> Status: **complete**  
> Product: **msgate**

---

## Summary

Digest emails now include the PDF attachment on Exchange. Previously MIME was built with a PDF, but the EWS sender only copied subject/body and dropped attachments.

---

## User guide

1. Account → Digests → **Send digest now** (or wait for schedule).
2. Inbox should show the short body **and** `msgate-*-digest.pdf`.

Requires working EWS credentials (same as capacity alerts).

---

## Technical

| Piece | Change |
| --- | --- |
| `ews/client.py` | `mime_file_attachments()` + `Message.attach(FileAttachment…)`; drafts folder before `send_and_save` when attachments present (Exchange &lt;2013 / on-prem) |
| Tests | `tests/test_ews_attachments.py` |

---

## Deferred (not in scope)

- Web UI self-upgrade / latest-version install
- Web UI data wipe / fresh install
